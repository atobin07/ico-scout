import { PagePlaceholder } from '@/components/dashboard/PagePlaceholder';

/** Geo map + technician tracking. Full build in Phase 6. */
export default function DispatchPage() {
  return (
    <PagePlaceholder
      title="Dispatch"
      description="Live technician map, ETAs, and today's schedule."
      phase="Phase 6"
    />
  );
}
