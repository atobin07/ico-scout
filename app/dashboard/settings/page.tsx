import { PagePlaceholder } from '@/components/dashboard/PagePlaceholder';

/** Business config + AI script editor. Full build alongside Phase 9. */
export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Business profile, AI script editor, and subscription."
      phase="Phase 9"
    />
  );
}
