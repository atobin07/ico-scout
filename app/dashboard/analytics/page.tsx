import { PagePlaceholder } from '@/components/dashboard/PagePlaceholder';

/** Revenue + call analytics. Full build in Phase 8. */
export default function AnalyticsPage() {
  return (
    <PagePlaceholder
      title="Analytics"
      description="Recovered revenue, call-to-booking rate, and peak-hour insights."
      phase="Phase 8"
    />
  );
}
