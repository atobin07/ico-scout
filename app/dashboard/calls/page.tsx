import { PagePlaceholder } from '@/components/dashboard/PagePlaceholder';

/** Call log + recordings. Full build alongside Phases 3 & 5. */
export default function CallsPage() {
  return (
    <PagePlaceholder
      title="Calls"
      description="Full call log with transcripts, recordings, and outcomes."
      phase="Phase 5"
    />
  );
}
