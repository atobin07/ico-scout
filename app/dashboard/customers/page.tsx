import { PagePlaceholder } from '@/components/dashboard/PagePlaceholder';

/** Customers CRM. Full build in Phase 7. */
export default function CustomersPage() {
  return (
    <PagePlaceholder
      title="Customers"
      description="Searchable CRM with call history, appointments, and notes."
      phase="Phase 7"
    />
  );
}
